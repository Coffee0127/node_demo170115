//- extends layout.pug
extends layout
//- content inserted into the layout.pug block content
block content
    //- 變數宣告
    - var friends = 10
    //- switch case
    case friedns
        when 0
            p you have no friends
        when 1
            p you have a friend
        default
            p you have #{friends} friends.
    //- for 迴圈
    - for (var x = 0; x < 3; x++)
        li item;
    a(class="button",href="http://www.google.com") hi